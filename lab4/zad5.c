#include <stdio.h>

int main() {

    int n, i;
    printf("Provide array length: ");
    scanf("%d", &n);

    int arr[n];

    int temp;
    for (i = 0; i <= n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("")

    return 0;
}
